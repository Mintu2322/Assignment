const { checkFirstChar } = require('./charstr');
const { evenElement } = require('./evenodd');
const { removeDuplicates } = require('./remove');
const { strMethod } = require('./strcase');


checkFirstChar(names);
evenElements(array2);
removeDuplicates(array);
letter(array);